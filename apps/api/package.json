{
  "name": "api",
  "private": true,
  "scripts": {
    "dev": "nodemon",
    "build": "tsup",
    "start": "NODE_ENV=production node dist",
    "check-types": "tsup --noEmit"
  },
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.2",
    "nodemon": "^3.1.10",
    "ts-node": "^10.9.2",
    "tsconfig-paths": "^4.2.0",
    "tsup": "^8.5.0"
  }
}
